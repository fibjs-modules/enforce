import { IValidator } from './common';
export declare function number(min: number, max: number, message?: string): IValidator;
export declare function length(min: number, max: number, message?: string): IValidator;
