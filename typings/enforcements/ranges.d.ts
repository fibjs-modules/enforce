import Validator from '../validator';
export declare function number(min: number, max: number, message?: string): Validator<{}>;
export declare function length(min: number, max: number, message?: string): Validator<{}>;
